/**
 * The SwaggerParameterType enum represents the type of a parameter in a Swagger specification.
 * @enum
 */
enum SwaggerParameterType {
  QUERY = 'query',
  PATH = 'path',
  header = 'header',
  BODY = 'body',
}

export default SwaggerParameterType;
